spring.application.name=backend

# Configurações de banco comentadas - descomente para usar com banco
# spring.datasource.url=jdbc:postgresql://localhost:5432/od46s_db
# spring.datasource.username=od46s_user
# spring.datasource.password=1234
# spring.datasource.driver-class-name=org.postgresql.Driver

# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.hibernate.ddl-auto=none
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true

# Liquibase Configuration (desabilitado - sem changelog)
# spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.yml
spring.liquibase.enabled=false
# spring.liquibase.drop-first=false

# Desabilitar spring session (não temos tabelas)
spring.session.store-type=none